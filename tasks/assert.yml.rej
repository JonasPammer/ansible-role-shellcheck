diff a/tasks/assert.yml b/tasks/assert.yml	(rejected hunks)
@@ -1,10 +1,10 @@
 ---
 # tasks file for testing that variables of ansible-role jonaspammer.shellcheck are set correctly

-- name: test if variable_name is set correctly
-  ansible.builtin.assert:
-    that:
-      - variable_name is defined
-      - variable_name is number
-      - variable_name >= 0
-    quiet: true
+# - name: test if variable_name is set correctly
+#   ansible.builtin.assert:
+#     that:
+#       - variable_name is defined
+#       - variable_name is number
+#       - variable_name >= 0
+#     quiet: true
